<Window x:Class="DonutMessager.Views.NewLoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Login"
        Width="350"
        Height="300"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>
    </Window.Resources>
    <Window.Background>
        <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
            <GradientStop Color="Black"/>
            <GradientStop Color="#FF1F1F1F" Offset="1"/>
        </LinearGradientBrush>
    </Window.Background>

    <StackPanel Margin="20">

        <TextBlock Text="Username" Foreground="White"/>
        <TextBox Text="{Binding Username}" Height="30"/>

        <Image Source="{Binding AvatarPath}"
               Height="80"
               Margin="0,10"
               Visibility="{Binding IsUserFound, Converter={StaticResource BoolToVisibility}}"/>

        <TextBlock Text="Password" Foreground="White"/>

        <PasswordBox x:Name="PasswordBox"
                     Height="30"
                     PasswordChanged="PasswordBox_OnPasswordChanged"
                     Visibility="{Binding IsUserFound, Converter={StaticResource BoolToVisibility}}"/>

        <Button Content="Login"
                Command="{Binding LoginCommand}"
                Height="35"
                Margin="0,10"/>

        <TextBlock Text="{Binding ErrorMessage}"
                   Foreground="Red"
                   FontWeight="Bold"/>
    </StackPanel>
</Window>